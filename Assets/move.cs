using UnityEngine;

public class move : MonoBehaviour
{
    [SerializeField] private jobjob a;
    // Start is called before the first frame update
    void Start()
    {
        a.move();
    }

    // Update is called once per frame
    void Update()
    {
    }
}
